torch>=2.4
transformers>=4.46
# Pinned exactly: scripts/train_grpo.py's risk-advantage patch is a full-method
# copy of GRPOTrainer._generate_and_score_completions from this trl version
# (rsp/_trl_patches/). Bumping this requires re-diffing that method and
# updating the patch -- see rsp/_trl_patches/README.md.
trl==1.10.0
peft>=0.13
# peft's LoRA dispatch always probes torchao's version and raises (rather than
# skipping) if it's too old -- Kaggle's base image ships 0.10.0, older than
# peft now requires, even though this repo never touches torchao-quantized
# layers.
torchao>=0.16.0
datasets>=3.0
accelerate>=1.0
numpy>=1.26
pyyaml>=6.0
pytest>=8.0
