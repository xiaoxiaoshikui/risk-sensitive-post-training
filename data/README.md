# Data

`dpo_pairs.jsonl` is generated from the SFT policy's own rollouts (a correct
completion chosen over an incorrect one for the same prompt) and is not
committed. Everything else is pulled from the HuggingFace Hub at runtime.
